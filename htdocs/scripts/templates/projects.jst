<container>
    <h2>Projects - <a class='action' href='#new_project'>New Project</a></h2>
    <projects>
    <% _.each(projects, function(project) { %>
	<%= JST['htdocs/scripts/templates/project']({ project: project }) %>
    <% }); %>
    </projects>
</container>
