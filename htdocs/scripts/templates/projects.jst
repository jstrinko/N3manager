<container>
    <h2>Projects</h2>
    <projects>
    <% _.each(projects, function(project) { %>
	<%= JST['htdocs/scripts/templates/project']({ project: project }) %>
    <% }); %>
    </projects>
</container>
